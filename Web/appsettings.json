{
  "Logging": { "LogLevel": { "Default": "Information", "Microsoft.AspNetCore": "Warning" } },
  "AllowedHosts": "*",

  "//ConnectionStrings": "Password is deliberately absent. Supply the full string via user-secrets locally (dotnet user-secrets set \"ConnectionStrings:SimpleERP\" \"...\"), or environment config in deployment. Never commit a real password.",
  "ConnectionStrings": {
    "SimpleERP": "Host=localhost;Port=5432;Database=simpleerp;Username=simpleerp"
  },

  "//Backup": "Optional explicit path to pg_dump.exe. If omitted, the newest C:\\Program Files\\PostgreSQL\\<ver>\\bin\\pg_dump.exe is used, falling back to PATH.",
  "Backup": {
    "PgDumpPath": ""
  }
}
